import { InsertPlayHololensAppRowDirective } from './insert-play-hololens-app-row.directive';

describe('InsertPlayHololensAppRowDirective', () => {
  it('should create an instance', () => {
    const directive = new InsertPlayHololensAppRowDirective();
    expect(directive).toBeTruthy();
  });
});
