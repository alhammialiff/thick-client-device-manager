import { InsertToastMessageDirective } from './insert-toast-message.directive';

describe('InsertToastMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new InsertToastMessageDirective();
    expect(directive).toBeTruthy();
  });
});
