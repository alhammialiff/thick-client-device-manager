import { InsertDevicePanelDirective } from './insert-device-panel.directive';

describe('InsertDevicePanelDirective', () => {
  it('should create an instance', () => {
    const directive = new InsertDevicePanelDirective();
    expect(directive).toBeTruthy();
  });
});
