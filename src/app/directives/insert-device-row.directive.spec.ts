import { InsertDeviceRowDirective } from './insert-device-row.directive';

describe('InsertDeviceRowDirective', () => {
  it('should create an instance', () => {
    const directive = new InsertDeviceRowDirective();
    expect(directive).toBeTruthy();
  });
});
